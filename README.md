Question 1:

jsx দিয়ে javascript এর মধ্যে HTML এর মতো কোড লিখা যায়। এটা করলে সব কোড সহজভাবে লিখা যায়।

Question 2:

State হচ্ছে কম্পোনেন্ট এর ভিতরের ডাটা যেটা কম্পোনেন্ট নিজেই পরিবর্তন করতে পারে।আবার ডাটা আপডেট ও করতে পারে।
Props দিয়ে ডাটা পাঠানো যায় প্যারেন্ট থেকে চাইল্ড এর মধ্যে।কিন্তু চাইল্ড ওই ডাটা চেঞ্জ করতে পারে না।

Question 3:

useState হচ্ছে একটা হুকের মতো।এটা দিয়ে স্টেট চেঞ্জ করা যায়। কোন ইনিশিয়াল স্টেট এর মধ্যে ডাটা থাকলে ওইটার স্টেট চেঞ্জ করা যায় setState দিয়ে।

Question 4:

স্টেট শেয়ার করার সবথেকে ভালো উপায় হচ্ছে স্টেট লিফট করা।প্যারেন্ট কম্পোনেন্ট থেকে চাইল্ড কম্পোনেন্ট এ প্রপ্স দিয়ে পাঠায় দেয়া যায়।


Question 5:

রিয়েক্ট এর মধ্যে ক্যামেল কেইস দিয়ে ইভেন্ট হ্যান্ডলিং করতে হয়। যেটা html এ নরমালি করলেই হয়ে যাইতো।